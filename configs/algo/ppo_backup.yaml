name: ppo
n_envs: 64
learning_rate: 3e-4
n_steps: 512
batch_size: 256
n_epochs: 10
gamma: 1.0          # undiscounted — V(s) converges to P(recover within episode | s)
gae_lambda: 1.0     # pure Monte-Carlo returns (no bias from bootstrapping)
clip_range: 0.2
ent_coef: 0.0
