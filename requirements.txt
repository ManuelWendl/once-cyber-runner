gymnasium
stable-baselines3[extra]
mujoco==3.3.0
numpy
Pillow
hydra-core>=1.3,<1.4
hydra-submitit-launcher==1.2.0
wandb
# DreamerV3 (dreamer/): torch is explicit now; tensordict/torchrl power the
# replay buffer (LazyTensorStorage + SliceSampler) and env batching. Install
# together with torch so pip resolves matching versions.
torch
tensordict
torchrl
